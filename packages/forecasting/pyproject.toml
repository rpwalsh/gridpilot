[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gridforge-forecasting"
version = "0.1.0"
description = "GridForge forecasting models"
requires-python = ">=3.11"
dependencies = ["gridforge-domain>=0.1.0", "walsh-predictive-core>=0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/gridforge_forecasting"]
