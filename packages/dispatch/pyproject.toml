[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gridforge-dispatch"
version = "0.1.0"
description = "GridForge battery dispatch optimizer"
requires-python = ">=3.11"
dependencies = ["gridforge-domain>=0.1.0", "walsh-predictive-core>=0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/gridforge_dispatch"]
