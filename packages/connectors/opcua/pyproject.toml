[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dispatchlayer-connector-opcua"
version = "0.1.0"
description = "DispatchLayer OPC UA read-only connector"
requires-python = ">=3.11"
dependencies = ["dispatchlayer-domain>=0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/dispatchlayer_connector_opcua"]
