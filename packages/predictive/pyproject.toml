# Proprietary (c) Ryan Walsh / Walsh Tech Group
# All rights reserved. Professional preview only.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dispatchlayer-predictive"
version = "0.1.0"
description = "DispatchLayer Predictive Operations Core: deterministic evidence-weighted reasoning for renewable operations intelligence"
requires-python = ">=3.11"
dependencies = ["dispatchlayer-domain>=0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/dispatchlayer_predictive"]
